// Base URL of the face-analysis server (see /server at the repo root).
// Point this at your deployed server before a release build; the Android
// emulator can't reach "localhost" on your machine, use 10.0.2.2 there.
export const API_BASE_URL = 'http://localhost:4000';
