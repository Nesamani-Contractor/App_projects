{
  "name": "shine-me-face-analysis-server",
  "version": "1.0.0",
  "private": true,
  "description": "Backend that holds the Perfect Corp / AWS API secrets and normalizes their responses for the Shine Me app. Must never ship inside the mobile bundle.",
  "main": "src/index.js",
  "type": "commonjs",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "dev": "node src/index.js",
    "start": "node src/index.js",
    "build-celebrity-collection": "node scripts/buildCelebrityCollection.js"
  },
  "dependencies": {
    "@aws-sdk/client-rekognition": "^3.700.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.1",
    "multer": "^2.0.0"
  }
}
